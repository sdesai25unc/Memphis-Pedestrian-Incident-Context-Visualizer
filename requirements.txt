# Python packages this project needs.
# Install with:  .\.venv\Scripts\python.exe -m pip install -r requirements.txt
requests==2.32.3
pandas==2.2.3

# Added in Phase 3+4 for the spatial join (roads + boundary classification).
# geopandas 1.x reads/writes GeoJSON through pyogrio, so fiona is not required.
geopandas==1.1.3
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.12.1
arcgis2geojson==3.1.1

# Optional — docs & verification tooling (not needed for the data pipeline):
#   docs/build_handoff_pdf.py (HANDOFF.pdf regeneration) and headless page checks.
#   playwright drives the locally installed Chrome (channel="chrome"); no browser download.
markdown==3.10.2
pypdf==6.14.2
playwright
python-docx
